'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = generateRule;

var _felaUtils = require('fela-utils');

function generateRule(selector, declaration) {
  var support = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : '';

  var rule = (0, _felaUtils.generateCSSRule)(selector, declaration);

  if (support.length > 0) {
    return (0, _felaUtils.generateCSSSupportRule)(support, rule);
  }

  return rule;
}